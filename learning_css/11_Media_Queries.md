//ANCHOR - Media queries are used to create responsive 

//ANCHOR - we write the conditioanl property and if those property satisfy we change the css

syntax:
@media not|only mediatype and (expression){
    CSS code:
}